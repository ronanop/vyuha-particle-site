# ElevenLabs Convai — required for /api/signed-url
ELEVENLABS_AGENT_ID=
ELEVENLABS_API_KEY=

# Clients must send this as Bearer token or X-Api-Key
SIGNED_URL_SECRET=

# Comma-separated browser origins allowed by CORS
CORS_ORIGINS=http://localhost:3000

PORT=4000
HOST=127.0.0.1
